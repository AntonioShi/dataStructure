//
// Created by niracler on 17-10-6.
//
/*

题目2：顺序循环队列的基本操作
        设队列的元素类型为char，实现顺序循环队列的各种基本操作的程序：
① 初始化队列Q；
② 判断队列Q是否为空；
③ 入队操作。循环调用入队操作，将若干元素（不少于10个）入队；
④ 出队操作，出队一个元素，并输出该元素；
⑤ 输出队列元素个数；
⑥ 调用入队操作，依次入队4个元素；
⑦ 输出队列序列；
⑧ 主函数通过函数调用实现以上各项操作。
 */
